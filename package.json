{
  "name": "acefloor-configurator",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @acefloor/web",
    "build": "npm run build --workspace @acefloor/web",
    "typecheck": "npm run typecheck --workspace @acefloor/web"
  }
}
